import { MovieclassificationPipe } from './movieclassification.pipe';

describe('MovieclassificationPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieclassificationPipe();
    expect(pipe).toBeTruthy();
  });
});

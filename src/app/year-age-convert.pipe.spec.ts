import { YearAgeConvertPipe } from './year-age-convert.pipe';

describe('YearAgeConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new YearAgeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
